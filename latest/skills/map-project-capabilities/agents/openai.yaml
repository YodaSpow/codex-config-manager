interface:
  display_name: "Map Project Capabilities"
  short_description: "Map reusable capabilities across selected projects"
  default_prompt: "Use $map-project-capabilities to map an approved project group for the current project, confirming scope before inspection and returning a read-only capability map in chat."
